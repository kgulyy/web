server {
    listen 80;

    location ^~ /uploads/ {
    	root /home/box/web/;
    }

    location ~* ^.+\.*$ {
    	root /home/box/web/;
    }

    location / {
        proxy_pass http://0.0.0.0:8000;
    }
}
